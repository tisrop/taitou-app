name: extended_image_lite
description: Lite version of extended_image with iOS-style physics for image gestures (bounce, inertia, snap).
version: 1.0.0

environment:
  sdk: ">=3.7.0 <4.0.0"
  flutter: ">=3.29.0"

resolution: workspace

dependencies:
  extended_image_library: ^5.0.0
  flutter:
    sdk: flutter
  meta: ^1.7.0
  vector_math: ^2.1.4
